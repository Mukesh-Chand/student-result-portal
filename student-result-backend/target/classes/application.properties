# ============================================================
# Student Result Portal - Application Configuration (Dev)
# ============================================================

spring.application.name=student-result-portal

# ----- Server -----
server.port=${PORT:8080}

# ----- PostgreSQL / Neon DB -----
spring.datasource.url=jdbc:postgresql://ep-broad-shadow-aoitcqri-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_nfA1WUREsZu3
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
# ----- JPA / Hibernate -----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ----- Admin Credentials (override via .env) -----
admin.username=Mukesh
admin.password=P@ssword4477

# ----- File Upload -----
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ----- Logging -----
logging.level.com.resultportal=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# ----- Rate Limiting -----
rate.limit.result.requests-per-minute=10
